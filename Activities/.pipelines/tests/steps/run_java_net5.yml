parameters:
  - name: buildPipeline
    type: string
  - name: packagesFolder
    type: string
  - name: studioInstallPath
    type: string
  - name: uipathTestDirectory
    type: string

steps:
   - task: DownloadPipelineArtifact@2
     displayName: Download artifact from build pipeline
     inputs:
      source: 'specific'
      project: 'Community'
      pipeline: ${{ parameters.buildPipeline }}
      runVersion: 'latest'
      path: ${{ parameters.packagesFolder }}

   - task: PowerShell@2
     displayName: Update dependencies from UiPath Dev and Run tests
     inputs:
      targetType: 'inline'
      ignoreLASTEXITCODE: true
      script: |
        cd ${{ parameters.packagesFolder }}
        $file = Get-ChildItem -Path .\ -Filter *.nupkg -Recurse -File -Name -Exclude *symbols*
        $dot = $file.LastIndexof('.')
        $file = $file.Replace(".nupkg","")
        $act = $file.LastIndexof('Activities.')
        $version = $file.Substring($act+11)
        Write-Host Update project to version $version
        Write-Host "##vso[build.updatebuildnumber]$version"
        
        $projectPath = "$(uipathTestDirectory)\NET_5\JavaNet5"
        mkdir -p $projectPath\XmlFiles

        cd ${{ parameters.studioInstallPath }}
        .\UiPath.Studio.CommandLine.exe manage --project-path "$projectPath\project.json" --add-ref "UiPath.Java.Activities|$version|strict" 
        dir "$projectPath\"

        Write-Host Publish project to folder $projectPath
        .\UiPath.Studio.CommandLine.exe publish --project-path "$projectPath\project.json" --target Custom --feed "$projectPath\" -v "$version"
        dir "$projectPath\"

        Write-Host Run nupkg file 
        .\uirobot.exe execute --file "$projectPath\JavaNet5.$version.nupkg" --input "{'XmlPath':'d:\\teste\\NET_5\\JavaNet5\\XmlFiles\\'}"

        cd $projectPath\
        dir "$projectPath\"
        dir "$projectPath\XmlFiles\"

        Write-Host Copy item "$projectPath\XmlFiles\"
        Copy-Item "$projectPath\XmlFiles\junit*.xml" -Destination "$(Build.ArtifactStagingDirectory)\junit_javaNet5.xml"

        Write-Host Remove item "$projectPath\XmlFiles\"
        Remove-Item "$projectPath\XmlFiles\junit*.xml"

   - task: PublishBuildArtifacts@1
     displayName: "Publish test files as Artifacts"
     inputs:
       PathtoPublish:  '$(Build.ArtifactStagingDirectory)'
       ArtifactName:   'Java Integration Tests for $(Build.BuildNumber)'
       ArtifactType:   'Container'

   - task: PublishTestResults@2
     displayName: "Publish test results as Test Runs output"
     inputs:
        testResultsFormat: 'JUnit'
        testResultsFiles: 'junit*.xml'
        searchFolder: '$(Build.ArtifactStagingDirectory)'
        testRunTitle: 'Java Integration Tests for $(Build.BuildNumber)'